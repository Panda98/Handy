<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.handy.support.mapper.customMapper.MyRecommendMapper">
    <resultMap id="UserItemLikeMap" type="com.handy.support.pojo.recommend.dto.UserItemLike">
        <result javaType="java.lang.Integer" property="userId" column="user_id" jdbcType="INTEGER"/>
        <result javaType="java.lang.Integer" property="itemId" column="item_id" jdbcType="INTEGER"/>
        <result javaType="java.lang.Float" property="preference" column="preference" jdbcType="FLOAT"/>
        <result javaType="java.util.Date" property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>
    <update id="updateRecord" parameterType="com.handy.support.pojo.recommend.dto.UserItemLike">
        update recommend
        set
                preference = #{preference,jdbcType=FLOAT},

                update_time = #{updateTime,jdbcType=TIMESTAMP}
       where user_id=#{userId} and item_id=#{itemId}
    </update>
    <select id="selectRecord" resultMap="UserItemLikeMap">
        select * from recommend where user_id=#{userId} and item_id=#{itemId}
    </select>
    <insert id="insertSelective" parameterType="com.handy.support.pojo.recommend.dto.UserItemLike">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        insert into recommend
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">
                user_id,
            </if>
            <if test="itemId != null">
                item_id,
            </if>
            <if test="updateTime != null">
                update_time,
            </if>
            <if test="preference != null">
                preference,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">
                #{userId,jdbcType=INTEGER},
            </if>
            <if test="itemId != null">
                #{itemId,jdbcType=INTEGER},
            </if>
            <if test="updateTime != null">
                #{updateTime,jdbcType=TIMESTAMP},
            </if>
            <if test="preference != null">
                #{preference,jdbcType=FLOAT},
            </if>
        </trim>
    </insert>
    <select id="getCoursesFromAlbum" resultType="java.lang.Integer">
        select course_id from album_course where album_id=#{albumId}  limit #{startRow},#{size}
    </select>
</mapper>