<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.handy.support.mapper.LikeMapper" >
    <resultMap id="BaseResultMap" type="com.handy.support.entity.Like" >
        <constructor >
            <arg column="user_id" jdbcType="INTEGER" javaType="java.lang.Integer" />
            <arg column="course_id" jdbcType="INTEGER" javaType="java.lang.Integer" />
        </constructor>
    </resultMap>
    <select id="isLiked">
        SELECT 1
        FROM `like` l
        WHERE user_id=#{userId} AND course_id=#{courseId} limit 1;
    </select>

    <delete id="deleteOne">
        delete from `like`
        WHERE user_id=#{userId} AND course_id=#{courseId};
    </delete>

</mapper>